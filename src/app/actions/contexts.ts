"use server"; import { requireAuthContext } from "@/lib/firebase/auth"; import { uploadWorkspaceFile } from "@/lib/firebase/storage"; export async function uploadContext(formData:FormData){const auth=await requireAuthContext();return uploadWorkspaceFile({workspaceId:auth.workspaceId,entityType:"context",entityId:String(formData.get("entityId")||"context"),content:String(formData.get("content")||""),authorId:auth.userId});}
